name: packer-validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  packer:
    name: Packer validate (${{ matrix.template }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      # max-parallel: 1 serializes the 6 matrix legs. Justification:
      # `packer init` makes GitHub API calls to resolve plugin tag refs.
      # Even with PACKER_GITHUB_API_TOKEN authenticating the calls, the
      # default GITHUB_TOKEN's quota (~1000 REST/hr/repo) gets exhausted
      # when 6 legs race in parallel -- saw 403 rate-limit failures on
      # commits dc5c588 (win11ent leg) and 19d8ae0 (deb13 leg). The
      # ~/.config/packer/plugins cache below makes the second leg onward
      # network-free, so serializing turns a 6-way concurrent rate-limit
      # battle into one fetch + five cache hits. Wall-clock cost is a
      # minute or two; reliability dominates.
      max-parallel: 1
      matrix:
        template:
          - nexus-gateway
          - deb13        # Phase 0.B.2
          - ubuntu24     # Phase 0.B.3
          - ws2025-core    # Phase 0.B.4
          - ws2025-desktop # Phase 0.B.5
          - win11ent       # Phase 0.B.6
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-packer@v3
        with:
          version: "1.11.2"

      # Cache the resolved plugin binaries across all legs + future runs.
      # Key off the .pkr.hcl files since required_plugins lives there;
      # restore-keys lets older snapshots seed new keys for incremental
      # plugin bumps.
      - uses: actions/cache@v4
        with:
          path: ~/.config/packer/plugins
          key: packer-plugins-${{ runner.os }}-${{ hashFiles('packer/**/*.pkr.hcl') }}
          restore-keys: |
            packer-plugins-${{ runner.os }}-

      - name: packer init
        working-directory: packer/${{ matrix.template }}
        # PACKER_GITHUB_API_TOKEN authenticates the GitHub API calls
        # `packer init` makes when resolving plugin versions. Combined
        # with the cache above and max-parallel:1, the first leg per run
        # may make a few API calls; subsequent legs hit the cache.
        env:
          PACKER_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: packer init .

      - name: packer fmt -check
        working-directory: packer/${{ matrix.template }}
        run: packer fmt -check -recursive .

      - name: packer validate
        working-directory: packer/${{ matrix.template }}
        # -syntax-only skips credential / network probe — CI has no VMware host
        run: packer validate -syntax-only .

  terraform:
    name: Terraform fmt + validate (${{ matrix.env }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        env:
          - gateway
          - deb13-smoke
          - ubuntu24-smoke
          - ws2025-core-smoke
          - ws2025-desktop-smoke
          - win11ent-smoke
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"
      - name: terraform fmt -check (repo-wide, once per matrix leg)
        run: terraform -chdir=terraform fmt -check -recursive
      - name: terraform init (no backend)
        run: terraform -chdir=terraform/${{ matrix.env }} init -backend=false
      - name: terraform validate
        run: terraform -chdir=terraform/${{ matrix.env }} validate

  ansible-lint:
    name: ansible-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ansible-lint ansible
      - run: ansible-lint packer/nexus-gateway/ansible/ packer/deb13/ansible/ packer/ubuntu24/ansible/ packer/_shared/ansible/

  shell-lint:
    name: ShellCheck + PSScriptAnalyzer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: ./scripts
          severity: warning
          ignore_names: '*.ps1'
      - name: PSScriptAnalyzer (Windows PowerShell provisioners + scripts)
        shell: pwsh
        run: |
          Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
          $targets = @(
            'packer/_shared/powershell/scripts',
            'packer/ws2025-desktop/scripts',
            'packer/win11ent/scripts',
            'scripts'
          ) | Where-Object { Test-Path $_ }
          $issues = foreach ($t in $targets) {
            Invoke-ScriptAnalyzer -Path $t -Recurse -Severity Warning,Error `
              -ExcludeRule PSAvoidUsingWriteHost,PSUseShouldProcessForStateChangingFunctions
          }
          if ($issues) {
            $issues | Format-Table -AutoSize | Out-String | Write-Host
            $errors = $issues | Where-Object Severity -eq 'Error'
            if ($errors) { throw "PSScriptAnalyzer: $($errors.Count) error(s)" }
          } else {
            Write-Host "PSScriptAnalyzer: clean"
          }

  gitleaks:
    name: gitleaks (secret scan)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so gitleaks can scan all commits on PRs
      - name: gitleaks detect
        uses: gitleaks/gitleaks-action@v2
        env:
          GITLEAKS_CONFIG: ${{ github.workspace }}/.gitleaks.toml
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: "false"
          GITLEAKS_ENABLE_SUMMARY: "true"

  product-key-guard:
    name: product-key pattern guard (pwsh)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan every tracked file
        shell: pwsh
        run: |
          $tracked = git ls-files
          & ./scripts/check-no-product-key.ps1 -Paths $tracked

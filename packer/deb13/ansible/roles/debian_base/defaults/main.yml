---
# debian_base — defaults
#
# Post-Phase 0.B.3 step 2: the generic variables (target_user, nic_name,
# ssh_allow_cidr) live on the shared roles (nexus_identity, nexus_network)
# and inherit from those. This file stays as an override hook for anything
# Debian-13-specific that might surface later.

debian_base_target_user: nexusadmin

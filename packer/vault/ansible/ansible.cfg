# ansible.cfg — vault template
# Same shape as deb13's ansible.cfg -- ansible-local uploads role_paths
# into a single directory on the target at Packer build time, so role
# references resolve by name. At lint time (CI), the shared roles need
# to be on the search path explicitly.
[defaults]
roles_path = roles:../../_shared/ansible/roles

---
# vault_node — defaults
# Overridable via Packer's --extra-vars (set from var.vault_version etc.)
vault_version: "1.18.4"
vault_arch: "linux_amd64"
vault_data_dir: /opt/vault/data
vault_config_dir: /etc/vault.d
vault_tls_dir: /etc/vault.d/tls
vault_user: vault
vault_group: vault
